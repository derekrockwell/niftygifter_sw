<!DOCTYPE html>
<html>
<head>
	<title>Nifty Gifter</title>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta name="description" content="Nifty Gifter helps you give the gifts they deserve and keep track of the things they want you to remember"/>
	<meta name="keywords" content="nifty gifter, wishlist, amazon, reminders, gift, present" />
	<meta name="author" content="">
	<link href='http://fonts.googleapis.com/css?family=Arbutus+Slab' rel='stylesheet' type='text/css'>
	<link href='http://fonts.googleapis.com/css?family=Lato' rel='stylesheet' type='text/css'>
	
	<%unless (request.fullpath == "/" && !user_signed_in?) %>
		<%= render 'layouts/jquery-mobile'%>
	<%else%>
		<%= render 'layouts/landing-page'%>
	<%end%>

	<%= render 'layouts/ieshim' %>
	<%= render 'layouts/analytics'%>
</head>
<body>
	<!-- load landing page -->
	<%if request.fullpath == "/" && !user_signed_in? %>
		<%= yield%>
	<%else%>
		<%= render 'layouts/mobile-header'%>
	<%end%>
</body>
</html>



